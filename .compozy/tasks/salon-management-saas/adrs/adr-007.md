# ADR-007: Frontend de Auto-Agendamento — Mesma SPA com Páginas Públicas

## Status

Accepted

## Date

2025-05-23

## Context

O PRD define auto-agendamento web via link compartilhável para clientes. A questão era se isso deveria ser parte da mesma SPA (Inertia) ou um frontend separado (mini app, subdomínio).

## Decision

Usar **mesma SPA Inertia** com **páginas públicas autenticadas por token público** (UUID):

- Páginas em `resources/js/Pages/Booking/` com layout público (sem auth Jetstream)
- Rota pública: `/booking/{tenant_slug}` → página de seleção de serviço/profissional/horário
- Autenticação via token UUID anexado ao link (`/booking/{tenant_slug}?token={uuid}`)
- Middleware `PublicBookingToken` valida token público na URL
- Para salão autenticado (dashboard admin): `/dashboard/calendar`, `/dashboard/clients`, etc.

## Alternatives Considered

### Alternative 1: Mini app separado (standalone Vue component)

- **Description**: Mini app Vue standalone para booking
- **Pros**: Totalmente isolado, pode ser deployado separadamente
- **Cons**: Duplicação de código (autenticação, componentes compartilhados). Mais complexidade de infraestrutura (2 builds). Menos consistente com app principal.
- **Why rejected**: Overhead de manter duas builds分开. A complexidade extra não justifica para MVP.

### Alternative 2: Subdomínio dedicado (booking.neezor.com)

- **Description**: SPA completa em subdomínio separado
- **Pros**: Total separação, CDN dedicado para assets públicos
- **Cons**: Múltiplos deployments, certificados SSL separados, configuração de DNS, mais trabalho DevOps. Overkill para MVP.
- **Why rejected**: Custo operacional alto. Não escala para multi-tenant genérico (seria `salão.neezor.com` ou similar).

## Consequences

### Positive

- Simples: mesma build, mesmo deploy, mesmo stack
- Componentes compartilhados (inputs, botões, layouts) reutilizados
- JWT público via link (sem login cliente, sem account management)
- Routing centralizado no mesmo arquivo
- Facilidade de desenvolvimento (não precisa trocar entre apps)

### Negative

- Payload do SPA inclui código de booking mesmo para quem só acessa dashboard
- Páginas de booking no mesmo bundle do admin dashboard
- Middleware de autenticação diferente para booking vs. admin (mais complexidade)

### Risks

- **Risco**: Token UUID vaza (é público por design)
  - **Mitigação**: Token por salão, não por cliente. Token rotacionável. Rate limiting em endpoints de booking.
- **Risco**: Bundle do SPA grande demais com páginas públicas
  - **Mitigação**: Lazy loading de páginas de booking com `defineAsyncComponent`

## Implementation Notes

- Middleware `PublicBookingToken` em `App/Http/Middleware/PublicBookingToken.php`
- Model `BookingToken` (tenant_id, token, expires_at, created_at)
- Páginas:
  - `resources/js/Pages/Booking/Index.vue` — seleção de serviço
  - `resources/js/Pages/Booking/Professional.vue` — seleção de profissional (opcional)
  - `resources/js/Pages/Booking/TimeSlot.vue` — seleção de horário
  - `resources/js/Pages/Booking/Confirm.vue` — confirmação final
- Layout `BookingLayout.vue` (minimal, sem Jetstream auth components)
- Build: `npm run build` — produção gera assets em `public/build`

## References

- PRD: F1.4 — Agendamento online; UX Journey do Cliente
- ADR-001: MVP approach — "Financeiro Completo"